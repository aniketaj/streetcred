



<div class="page-header">
  <h3>
    FEEDBACK RECEIVED
    <!--<a href="/critiques/new" class="btn btn-lg btn-success">New Critique</a> -->
  </h3>
</div>


<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover table-responsive">
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Collaboration</th>
        <th>Reliability</th>
        <th>Communication</th>
        <th>Committment</th>
        <th>TOTAL SCORE</th>
    <!--    <th>User</th> -->

        <th class="col-sm-2"> Actions</th>
      </tr>

      <% @receivedfbs.first(3).each do |critique| %>


      <tr>
        <td><strong><%= critique.name %></strong></td>
        <td><%= critique.description %></td>
        <td><strong><%= critique.metricone %></strong></td>
        <td><strong><%= critique.metrictwo %></strong></td>
        <td><strong><%= critique.metricthree %></strong></td>
        <td><strong><%= critique.metricfour %></strong></td>
        <td><strong><%= critique.metricfive %></strong></td>
      <!--  <td><%= critique.user_id %></td> -->
        <td>
          <a href="/critiques/<%= critique.id %>" class="btn btn-sm btn-primary">Show</a>
        <!--  <a href="/critiques/<%= critique.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_critique/<%= critique.id %>" class="btn btn-danger" rel="nofollow">Delete</a> -->
        </td>
      </tr>

      <% end %>
    </table>
  </div>
</div>
<hr>


  <div class="form-group">
      <a href="/users" class="btn btn-md btn-primary">Find Friends and Provide Feedback</a>
    </div>
    <hr>
    <div class="form-group">
      <h3>
        FEEDBACK PROVIDED BY ME
        <!--<a href="/critiques/new" class="btn btn-lg btn-success">New Critique</a> -->
      </h3>
    </div>



  <div class="row">
    <div class="col-md-12">
      <table class="table table-striped table-hover table-responsive">
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Collaboration</th>
          <th>Reliability</th>
          <th>Communication</th>
          <th>Committment</th>
          <th>TOTAL SCORE</th>
      <!--    <th>User</th> -->

          <th class="col-sm-2"> Actions</th>
        </div>
        </tr>

        <% @critiques.first(3).each do |critique| %>
        <tr>
          <td><strong><%= critique.name %></strong></td>
          <td><%= critique.description %></td>
          <td><strong><%= critique.metricone %></strong></td>
          <td><strong><%= critique.metrictwo %></strong></td>
          <td><strong><%= critique.metricthree %></strong></td>
          <td><strong><%= critique.metricfour %></strong></td>
          <td><strong><%= critique.metricfive %></strong></td>
        <!--  <td><%= critique.user_id %></td> -->
          <td>
            <a href="/critiques/<%= critique.id %>" class="btn btn-xs btn-primary">Show</a>
            <a href="/critiques/<%= critique.id %>/edit" class="btn btn-xs btn-warning">Edit</a>
            <a href="/delete_critique/<%= critique.id %>" class="btn btn-xs btn-danger" rel="nofollow">Delete</a>
          </td>
        </tr>
        <% end %>
      </table>
    </div>

</div>
