<div class="page-header">
  <h3>
    MY RECEIVED? FEEDBACKS
    <!--<a href="/critiques/new" class="btn btn-lg btn-success">New Critique</a> -->
  </h3>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Collaboration</th>
        <th>Reliability</th>
        <th>Communication</th>
        <th>Committment</th>
        <th>TOTAL SCORE</th>
    <!--    <th>User</th> -->

        <th>Actions</th>
      </tr>

      <% @receivedfbs.each do |critique| %>

      <tr>
        <td><strong><%= critique.name %></strong></td>
        <td><%= critique.description %></td>
        <td><strong><%= critique.metricone %></strong></td>
        <td><strong><%= critique.metrictwo %></strong></td>
        <td><strong><%= critique.metricthree %></strong></td>
        <td><strong><%= critique.metricfour %></strong></td>
        <td><strong><%= critique.metricfive %></strong></td>
      <!--  <td><%= critique.user_id %></td> -->
        <td>
          <a href="/critiques/<%= critique.id %>" class="btn btn-primary">Show</a>
        <!--  <a href="/critiques/<%= critique.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_critique/<%= critique.id %>" class="btn btn-danger" rel="nofollow">Delete</a> -->
        </td>
      </tr>

      <% end %>
    </table>
  </div>

  <div class="page-header">
    <h3>
      FEEDBACKS BY ME
      <!--<a href="/critiques/new" class="btn btn-lg btn-success">New Critique</a> -->
    </h3>
  </div>

  <div class="row">
    <div class="col-md-12">
      <table class="table table-striped table-hover">
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Collaboration</th>
          <th>Reliability</th>
          <th>Communication</th>
          <th>Committment</th>
          <th>TOTAL SCORE</th>
      <!--    <th>User</th> -->

          <th>Actions</th>
        </tr>

        <% @critiques.each do |critique| %>
        <tr>
          <td><strong><%= critique.name %></strong></td>
          <td><%= critique.description %></td>
          <td><strong><%= critique.metricone %></strong></td>
          <td><strong><%= critique.metrictwo %></strong></td>
          <td><strong><%= critique.metricthree %></strong></td>
          <td><strong><%= critique.metricfour %></strong></td>
          <td><strong><%= critique.metricfive %></strong></td>
        <!--  <td><%= critique.user_id %></td> -->
          <td>
            <a href="/critiques/<%= critique.id %>" class="btn btn-primary">Show</a>
            <a href="/critiques/<%= critique.id %>/edit" class="btn btn-warning">Edit</a>
            <a href="/delete_critique/<%= critique.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
          </td>
        </tr>
        <% end %>
      </table>
    </div>

<tr>
  <h3>
    PROVIDE FEEDBACK </h3>
  </tr>

    <a href="/users" class="btn btn-lg btn-primary">Find Friends and Provide Feedback</a>

</div>
